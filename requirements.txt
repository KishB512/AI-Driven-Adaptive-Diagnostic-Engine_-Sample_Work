# FastAPI - Web framework used to build the REST API for the adaptive GRE testing system
fastapi

# Uvicorn - ASGI server used to run the FastAPI application
uvicorn

# PyMongo - MongoDB driver used to connect Python with the MongoDB database
pymongo

# Pydantic - Used for request validation and data models in FastAPI
pydantic

# Groq - Client library used to access Groq LLM API for generating personalized learning plans
groq

# python-dotenv - Loads environment variables (like API keys) from a .env file
python-dotenv